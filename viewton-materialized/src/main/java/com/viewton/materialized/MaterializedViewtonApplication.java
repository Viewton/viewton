package com.viewton.materialized;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Application entry point for the materialized Viewton module.
 */
@SpringBootApplication
public class MaterializedViewtonApplication {
    public static void main(String[] args) {
        SpringApplication.run(MaterializedViewtonApplication.class, args);
    }
}
